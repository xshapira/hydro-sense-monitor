{
	"${SERVICE_NAME}": {
		"image": ":${SERVICE_NAME}.${SERVICE_NAME}.latest",
		"ports": {
			"${SERVICE_PORT}": "HTTP"
		},
		"environment": {
			"VITE_SHOW_DEV_TOOLS": "${VITE_SHOW_DEV_TOOLS}"
		}
	}
}
